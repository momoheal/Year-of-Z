# 第三章 Demo 实施记录：今天不煮了 · 写在背面（含首次遭遇战）

状态：2026-09-24 接入。本轮把第三章正文（doc/08、doc/09）做成可玩节点，并实现全作**第一处战斗**——
东街临时厨房的自卫遭遇。正文未改动；本文件记录实现范围、战斗设计、红线自检与遗留项。

## 本轮要求

用户要求："继续第三章的开发，最好缩短非战斗流程，尽快进入战斗。"
因此本章的铺垫被压到最短：**三个节点之内进入战斗**，且三个节点各自只有一次交互、不设跑腿支线。

## 节点表（6 个，`src/data/chapter3.ts`）

| 节点 | 场景 | 内容 | 结束时 |
| --- | --- | --- | --- |
| C03-00 今天不煮了 | yard | 电话、街道安排、上社区小货车（背包里没有撬棍也没有刀） | → dongjie |
| C03-01 十七只碗 | dongjie | 铁网门交接、十七份餐来了十九个人（二选一：让出干粮 / 先记到场再报缺口） | → kitchen |
| C03-02 那间小屋 | kitchen | 值班室的椅腿声、待转运单、医生要求不要开门；冯师傅把手搭上门闩 | 门被拉开 |
| **C03-03 那把刀就在手边** | kitchen | **遭遇战**（`encounter: 'kitchen'`，进场即打）；打完自动播放善后段落 | → obsroom |
| C03-04 写在背面 | obsroom | 观察处的夜、母亲电话、补充询问；"咬伤"必须更正，照原样签会被退回 | 签字 |
| C03-05 临时三天 | obsroom | 冯师傅的语音、二十二人名单、临时接三天 | 章节完 |

非战斗流程用时：三段对话 + 两段几十米的步行；进入厨房后走到值班室门即开打。

## 遭遇战设计（`src/combat.ts` + `world.ts` 渲染 + `main.ts` 输入）

内核是**纯函数状态机**（不依赖 DOM / three.js），因此可以整场用例化跑（见 `tests/combat.test.ts` 14 例）。

- **顺序是故事定死的**：退 → 抓椅子 → 椅子散架 → 被逼到备餐台 → 刀。
  手里举着椅子时拿不到刀；一开始跑去备餐台也拿不到——他还没有想到刀。
- **动作**：移动/冲刺（耗体力）、按住空格（右键 / 触屏「挡」）抬臂挡、J（左键 / 触屏「挥」）挥、E（触屏「拿」）拿手边的东西。
- **来人（冯志远）**：只有一个，行为是 追 → 起势 → 扑 → 收势；扑出去的方向在起跳瞬间锁定，可以被走位躲开。
- **挡的三种结果**：有椅子＝挡开并把他撞退（椅子只扛两下）；空手＝勉强推开一次（很耗体力）；有刀＝抵住一瞬（`clinch`，1.5 秒内挥出去才结束）。
- **被扑住**：不是掉血，是被按住——连按挣开（5 下 / 2.4 秒）；挣不开就是失败。
- **失败**：不是死亡演出，是"他把你按在了地上"，可以从门被拉开那一刻**重来**（不写任何剧情事实、不计战绩）。
- **结束即结束**：`outcome = 'win'` 之后状态机冻结，不能补刀、不能追击、不再产生任何事件。
- **引导**：地面引路箭头与 E 提示始终指向"下一件能拿的东西"；HUD 只有一条体力与一句提示，没有敌人血条。

数值（`TUNING`）：来人 3.05 m/s、扑 7.4 m/s；玩家走 3.3 / 跑 5.2（跑耗 21/s，回 15/s）。
脚本玩家（会退、会挡、会拿）跑完一场约 **17 秒**，作为可玩性回归用例固定下来（>8 秒且 <75 秒）。

## 新增场景

- `kitchen`（精细建图）：长桌与十七只碗（含缠胶带的那只）、备餐台与砧板上的刀和半截白萝卜、
  窗下拔了插头的电饭锅、褪色的歌咏比赛合照、糊着报纸的值班室门。物理与战斗遮挡共用 `mapdata.KITCHEN_WALLS`。
- `dongjie`、`obsroom`：沿用通用占位建图 + 专用道具（铁网门与压缺口的砖、墙边那辆绑着两只塑料筐的电动车；
  观察处的床、收了一半的投影幕、通风门边的询问桌）。

## 红线自检（对照 doc/00、doc/23、doc/24）

- 没有枪：全章无枪械物品、无射击；工坊仍与章节状态隔离。
- 刀不是许晨的，也**没有进过背包**：现场留作记录与调查，未归还（用例固定）。
- 只有一名来人，**不可连杀、不可追击、不可补刀**；没有击杀数、没有战果结算、没有胜利音效。
- 不写咬伤特写、不写血腥细节；倒下只有一个"靠着台沿滑下去"的剪影。
- 本章不揭示自愈；冯师傅的暴露评估不给结论；"他是不是还会起来"这句问句留在原地，没有人回答。
- 十九人是"本次到场"，二十二人是"逐户核实后需配送"，两个数字不相减。
- 陈述环节：未亲见的"咬伤"不能签字确认；记不全的几秒必须写成"顺序不确定"。

## 试玩入口（仅为试玩与回归，不改变正式流程）

标题画面新增「第三章试玩 · 直奔东街」：把一、二章按默认选择补全，直接从 C03-00 开始（清除旧档）。
地址参数 `?jump=c3` 等效；`?jump=fight` 则补全到 C03-02，进页面就站在厨房里，门随即被拉开。
补全写入的是同一套存档结构，不存在"只给按钮不给状态"的假进度。

## 验证

- `npx tsc --noEmit`：通过。
- `npm test`：31 例通过（story 17 + combat 14），其中第三章 4 例、战斗 14 例为本轮新增。
- `npm run build`：通过（three 475 kB / gzip 120 kB，无警告）。
- 存档：`SAVE_VERSION` 升到 3，v1 → v2 → v3 迁移链保留；战斗过程不进存档，
  读档时若当前节点是 C03-03，则从门被拉开那一刻重新开打。

## 如实记录的未完成项

- **浏览器实机走查仍未执行**：本环境无法下载 Chromium（`npx playwright install chromium` 失败），
  `npm run verify` 与手感调校（走位余量、扑击提前量、连按手感、触屏三键位置）需在有浏览器的环境完成。
- `scripts/verify.mjs` 尚未覆盖第三章遭遇战流程（仍是第一章脚本）。
- 战斗动画是积木人仔的程序化姿态（前伸双臂、踉跄、倒地），没有专门的受击/挣扎动作。
- `dongjie` 与 `obsroom` 仍是占位建图 + 道具，未做精细室内外环境。
- 音频为合成占位：撞击、摩擦、呼吸，均为低频噪声，没有人声。
